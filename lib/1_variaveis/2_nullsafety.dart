void main() {
  // null serve para definir uma variavel nula, para isso coloque '?' depois da variavel
  String? nomeCompleto;
  // para forçar uma variavel a não ser nula, usar o '!' depois dela definida
  print(nomeCompleto!.length);
  // EVITAR AO MÁXIMO UTILIZAR O '!'
  // EVITAR TAMBEM AO MÁXIMO VARIAVEIS NULAS
}
